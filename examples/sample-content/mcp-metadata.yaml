server:
  name: MCP Acdc Server (Go)
  version: 1.0.0
  instructions: |
    MCP ACDC provides development resources and tools through the Model Context Protocol.

    This server demonstrates the Docker content mount pattern, where content is provided
    via a separate content image that can be versioned and deployed independently.

    Use resources for broad context (standards, guidelines, best practices).
    Use the search tool to find specific information across all resources.

